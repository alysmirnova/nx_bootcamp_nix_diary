# ƒомашнее задание є 1: Linux и bash shell

## ”становка

ƒл€ установки программы необходимо скачать все файлы, затем выполнить в командной строке : ```bash compile.sh```.

##  оманды

```bash diary.sh help ``` - вызов справки.
```bash diary.sh add``` - создание новой записи.
```bash diary.sh add -t``` - создание записи с выбором шаблона.
```bash diary.sh config -d``` - изменение каталога дл€ сохранени€ записей.
```bash diary.sh config -e``` - изменение текстового редактора дл€ открыти€ по умолчанию.
```bash diary.sh delete``` - удаление записи.
```bash diary.sh restore``` - восстановление записи из корзины.
```bash diary.sh stats``` - статистика.
```bash diary.sh template ``` - создание шаблона.